class Snowman {
  constructor(x, y) {
    this.posX = x;
    this.posY = y;
    /**
     * @description Draws the snowman using P5 Drawing functions
     */
    this.drawBody = function () {
      // Helpful function fill(red, green, blue);
      // Helpful function ellipse(x, y, width, height);
    }
    /**
     * Draws a snowman arm
     * @param {Boolean} isRightArm Flag to determine which arm is being drawn
     */
    this.drawArm = function (isRightArm) {
      // Helpful function stroke(red, green, blue);
      // Helpful function strokeWeight(wght);
      // Helpful function line(x0, y0, x1, y1);
    }
    /**
     * @description Draws full snowman
     */
    this.drawArm = function () {
      
    }
  }

}

